const { getSpecificResult } = require("./utility");
const { getManyResult } = require("./utility");

const homeDatamapper = {
//! inserer les fonctions async avec leurs requêtes SQL
};

module.exports = homeDatamapper;
